platform :ios, '15.0'

target 'snap capsule' do
  use_frameworks!

  # Google Cloud Vision API
  pod 'GoogleCloudVision'
  
  # For handling JSON
  pod 'SwiftyJSON'
  
  target 'snap capsuleTests' do
    inherit! :search_paths
  end

  target 'snap capsuleUITests' do
  end
end 